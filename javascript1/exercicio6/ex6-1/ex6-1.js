let mult = prompt("Qual o valor que vai querer multiplicar? ");

for (let tab = 0; tab <= 20; tab++) {
  let resultado = mult * tab;
  alert("O resultado da sua multiplicação foi igual a: " + resultado);
}
